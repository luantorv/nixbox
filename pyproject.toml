[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "nixbox"
version = "0.1.0"
description = "Sandboxed Agent Management Layer for NixOS"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlmodel",
    "apscheduler",
    "python-multipart",
    "aiofiles",
]

[project.scripts]
nixbox = "nixbox.main:run"

[tool.setuptools.packages.find]
where = ["."]
include = ["nixbox*"]